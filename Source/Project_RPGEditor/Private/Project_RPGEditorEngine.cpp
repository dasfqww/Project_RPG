#include "Project_RPGEditorEngine.h"

void UProject_RPGEditorEngine::Init(IEngineLoop* InEngineLoop)
{
	Super::Init(InEngineLoop);

	// 여기에 초기 설정 로직 추가 (예: 플러그인 폴더 가시성 설정 등)
}

void UProject_RPGEditorEngine::Tick(float DeltaSeconds, bool bIdleMode)
{
	Super::Tick(DeltaSeconds, bIdleMode);
}
